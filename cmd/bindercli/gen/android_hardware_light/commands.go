// Code generated by spec2cli. DO NOT EDIT.

//go:build linux

package android_hardware_light

import (
	"context"
	"fmt"
	"os"

	"github.com/AndroidGoLab/binder/binder"
	"github.com/AndroidGoLab/binder/cmd/bindercli/cliutil"
	"github.com/spf13/cobra"

	"github.com/AndroidGoLab/binder/android/hardware/light"
)

// Register adds all generated commands for this AIDL package to root.
func Register(root *cobra.Command) {
	root.AddCommand(newCmdAndroidHardwareLightILights())
}

func newCmdAndroidHardwareLightILights() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "android.hardware.light.ILights",
		Short: "android.hardware.light.ILights methods",
	}

	cmd.AddCommand(newCmdAndroidHardwareLightILights_SetLightState())
	cmd.AddCommand(newCmdAndroidHardwareLightILights_GetLights())

	return cmd
}

func newCmdAndroidHardwareLightILights_SetLightState() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "set-light-state",
		Short: "Call SetLightState(id int32, state HwLightState)",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()

			conn, err := cliutil.OpenConn(ctx, cmd)
			if err != nil {
				return err
			}
			defer conn.Close(ctx)

			serviceName, _ := cmd.Flags().GetString("service-name")
			var svc binder.IBinder
			if serviceName != "" {
				svc, err = conn.GetService(ctx, serviceName)
			} else {
				svc, err = cliutil.FindServiceByDescriptor(ctx, conn, "android.hardware.light.ILights")
			}
			if err != nil {
				return err
			}

			svcProxy := light.NewLightsProxy(svc)

			flagId, err := cmd.Flags().GetInt32("id")
			if err != nil {
				return fmt.Errorf("reading flag id: %w", err)
			}

			var flagState light.HwLightState
			flagState.Color, _ = cmd.Flags().GetInt32("state-color")
			_state_flash_mode, _ := cmd.Flags().GetInt32("state-flash-mode")
			flagState.FlashMode = light.FlashMode(_state_flash_mode)
			flagState.FlashOnMs, _ = cmd.Flags().GetInt32("state-flash-on-ms")
			flagState.FlashOffMs, _ = cmd.Flags().GetInt32("state-flash-off-ms")
			_state_brightness_mode, _ := cmd.Flags().GetInt32("state-brightness-mode")
			flagState.BrightnessMode = light.BrightnessMode(_state_brightness_mode)

			err = svcProxy.SetLightState(ctx, flagId, flagState)
			if err != nil {
				return err
			}

			mode, _ := cmd.Root().PersistentFlags().GetString("format")
			f := cliutil.NewFormatter(mode, os.Stdout)
			f.Value("status", "ok")
			return nil
		},
	}

	cmd.Flags().String("service-name", "", "ServiceManager name to use instead of descriptor discovery")
	cmd.Flags().Int32("id", 0, "id (int32)")
	_ = cmd.MarkFlagRequired("id")
	cmd.Flags().Int32("state-color", 0, "state.Color (int32)")
	cmd.Flags().Int32("state-flash-mode", 0, "state.FlashMode (enum)")
	cmd.Flags().Int32("state-flash-on-ms", 0, "state.FlashOnMs (int32)")
	cmd.Flags().Int32("state-flash-off-ms", 0, "state.FlashOffMs (int32)")
	cmd.Flags().Int32("state-brightness-mode", 0, "state.BrightnessMode (enum)")

	return cmd
}

func newCmdAndroidHardwareLightILights_GetLights() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "get-lights",
		Short: "Call GetLights() -> []HwLight",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()

			conn, err := cliutil.OpenConn(ctx, cmd)
			if err != nil {
				return err
			}
			defer conn.Close(ctx)

			serviceName, _ := cmd.Flags().GetString("service-name")
			var svc binder.IBinder
			if serviceName != "" {
				svc, err = conn.GetService(ctx, serviceName)
			} else {
				svc, err = cliutil.FindServiceByDescriptor(ctx, conn, "android.hardware.light.ILights")
			}
			if err != nil {
				return err
			}

			svcProxy := light.NewLightsProxy(svc)

			result, err := svcProxy.GetLights(ctx)
			if err != nil {
				return err
			}

			mode, _ := cmd.Root().PersistentFlags().GetString("format")
			f := cliutil.NewFormatter(mode, os.Stdout)
			f.Value("result", result)
			return nil
		},
	}

	cmd.Flags().String("service-name", "", "ServiceManager name to use instead of descriptor discovery")

	return cmd
}
