// Code generated by spec2cli. DO NOT EDIT.

//go:build linux

package android_hardware_authsecret

import (
	"context"
	"encoding/hex"
	"fmt"
	"os"

	"github.com/AndroidGoLab/binder/binder"
	"github.com/AndroidGoLab/binder/cmd/bindercli/cliutil"
	"github.com/spf13/cobra"

	"github.com/AndroidGoLab/binder/android/hardware/authsecret"
)

// Register adds all generated commands for this AIDL package to root.
func Register(root *cobra.Command) {
	root.AddCommand(newCmdAndroidHardwareAuthsecretIAuthSecret())
}

func newCmdAndroidHardwareAuthsecretIAuthSecret() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "android.hardware.authsecret.IAuthSecret",
		Short: "android.hardware.authsecret.IAuthSecret methods",
	}

	cmd.AddCommand(newCmdAndroidHardwareAuthsecretIAuthSecret_SetPrimaryUserCredential())

	return cmd
}

func newCmdAndroidHardwareAuthsecretIAuthSecret_SetPrimaryUserCredential() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "set-primary-user-credential",
		Short: "Call SetPrimaryUserCredential(secret []byte)",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()

			conn, err := cliutil.OpenConn(ctx, cmd)
			if err != nil {
				return err
			}
			defer conn.Close(ctx)

			serviceName, _ := cmd.Flags().GetString("service-name")
			var svc binder.IBinder
			if serviceName != "" {
				svc, err = conn.GetService(ctx, serviceName)
			} else {
				svc, err = cliutil.FindServiceByDescriptor(ctx, conn, "android.hardware.authsecret.IAuthSecret")
			}
			if err != nil {
				return err
			}

			svcProxy := authsecret.NewAuthSecretProxy(svc)

			flagSecretHex, err := cmd.Flags().GetString("secret")
			if err != nil {
				return fmt.Errorf("reading flag secret: %w", err)
			}
			flagSecret, err := hex.DecodeString(flagSecretHex)
			if err != nil {
				return fmt.Errorf("invalid hex for secret: %w", err)
			}

			err = svcProxy.SetPrimaryUserCredential(ctx, flagSecret)
			if err != nil {
				return err
			}

			mode, _ := cmd.Root().PersistentFlags().GetString("format")
			f := cliutil.NewFormatter(mode, os.Stdout)
			f.Value("status", "ok")
			return nil
		},
	}

	cmd.Flags().String("service-name", "", "ServiceManager name to use instead of descriptor discovery")
	cmd.Flags().String("secret", "", "secret (hex bytes)")
	_ = cmd.MarkFlagRequired("secret")

	return cmd
}
