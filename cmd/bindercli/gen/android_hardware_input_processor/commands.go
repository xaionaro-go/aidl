// Code generated by spec2cli. DO NOT EDIT.

//go:build linux

package android_hardware_input_processor

import (
	"context"
	"fmt"
	"os"

	"github.com/AndroidGoLab/binder/binder"
	"github.com/AndroidGoLab/binder/cmd/bindercli/cliutil"
	"github.com/spf13/cobra"

	"github.com/AndroidGoLab/binder/android/hardware/input/processor"
)

// Register adds all generated commands for this AIDL package to root.
func Register(root *cobra.Command) {
	root.AddCommand(newCmdAndroidHardwareInputProcessorIInputProcessor())
}

func newCmdAndroidHardwareInputProcessorIInputProcessor() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "android.hardware.input.processor.IInputProcessor",
		Short: "android.hardware.input.processor.IInputProcessor methods",
	}

	cmd.AddCommand(newCmdAndroidHardwareInputProcessorIInputProcessor_Reset())
	cmd.AddCommand(newCmdAndroidHardwareInputProcessorIInputProcessor_ResetDevice())

	return cmd
}

func newCmdAndroidHardwareInputProcessorIInputProcessor_Reset() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "reset",
		Short: "Call Reset()",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()

			conn, err := cliutil.OpenConn(ctx, cmd)
			if err != nil {
				return err
			}
			defer conn.Close(ctx)

			serviceName, _ := cmd.Flags().GetString("service-name")
			var svc binder.IBinder
			if serviceName != "" {
				svc, err = conn.GetService(ctx, serviceName)
			} else {
				svc, err = cliutil.FindServiceByDescriptor(ctx, conn, "android.hardware.input.processor.IInputProcessor")
			}
			if err != nil {
				return err
			}

			svcProxy := processor.NewInputProcessorProxy(svc)

			err = svcProxy.Reset(ctx)
			if err != nil {
				return err
			}

			mode, _ := cmd.Root().PersistentFlags().GetString("format")
			f := cliutil.NewFormatter(mode, os.Stdout)
			f.Value("status", "ok")
			return nil
		},
	}

	cmd.Flags().String("service-name", "", "ServiceManager name to use instead of descriptor discovery")

	return cmd
}

func newCmdAndroidHardwareInputProcessorIInputProcessor_ResetDevice() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "reset-device",
		Short: "Call ResetDevice(deviceId int32)",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()

			conn, err := cliutil.OpenConn(ctx, cmd)
			if err != nil {
				return err
			}
			defer conn.Close(ctx)

			serviceName, _ := cmd.Flags().GetString("service-name")
			var svc binder.IBinder
			if serviceName != "" {
				svc, err = conn.GetService(ctx, serviceName)
			} else {
				svc, err = cliutil.FindServiceByDescriptor(ctx, conn, "android.hardware.input.processor.IInputProcessor")
			}
			if err != nil {
				return err
			}

			svcProxy := processor.NewInputProcessorProxy(svc)

			flagDeviceId, err := cmd.Flags().GetInt32("deviceId")
			if err != nil {
				return fmt.Errorf("reading flag deviceId: %w", err)
			}

			err = svcProxy.ResetDevice(ctx, flagDeviceId)
			if err != nil {
				return err
			}

			mode, _ := cmd.Root().PersistentFlags().GetString("format")
			f := cliutil.NewFormatter(mode, os.Stdout)
			f.Value("status", "ok")
			return nil
		},
	}

	cmd.Flags().String("service-name", "", "ServiceManager name to use instead of descriptor discovery")
	cmd.Flags().Int32("deviceId", 0, "deviceId (int32)")
	_ = cmd.MarkFlagRequired("deviceId")

	return cmd
}
