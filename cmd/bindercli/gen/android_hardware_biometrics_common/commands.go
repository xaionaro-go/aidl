// Code generated by spec2cli. DO NOT EDIT.

//go:build linux

package android_hardware_biometrics_common

import (
	"context"
	"os"

	"github.com/AndroidGoLab/binder/binder"
	"github.com/AndroidGoLab/binder/cmd/bindercli/cliutil"
	"github.com/spf13/cobra"

	"github.com/AndroidGoLab/binder/android/hardware/biometrics/common"
)

// Register adds all generated commands for this AIDL package to root.
func Register(root *cobra.Command) {
	root.AddCommand(newCmdAndroidHardwareBiometricsCommonICancellationSignal())
}

func newCmdAndroidHardwareBiometricsCommonICancellationSignal() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "android.hardware.biometrics.common.ICancellationSignal",
		Short: "android.hardware.biometrics.common.ICancellationSignal methods",
	}

	cmd.AddCommand(newCmdAndroidHardwareBiometricsCommonICancellationSignal_Cancel())

	return cmd
}

func newCmdAndroidHardwareBiometricsCommonICancellationSignal_Cancel() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "cancel",
		Short: "Call Cancel()",
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()

			conn, err := cliutil.OpenConn(ctx, cmd)
			if err != nil {
				return err
			}
			defer conn.Close(ctx)

			serviceName, _ := cmd.Flags().GetString("service-name")
			var svc binder.IBinder
			if serviceName != "" {
				svc, err = conn.GetService(ctx, serviceName)
			} else {
				svc, err = cliutil.FindServiceByDescriptor(ctx, conn, "android.hardware.biometrics.common.ICancellationSignal")
			}
			if err != nil {
				return err
			}

			svcProxy := common.NewCancellationSignalProxy(svc)

			err = svcProxy.Cancel(ctx)
			if err != nil {
				return err
			}

			mode, _ := cmd.Root().PersistentFlags().GetString("format")
			f := cliutil.NewFormatter(mode, os.Stdout)
			f.Value("status", "ok")
			return nil
		},
	}

	cmd.Flags().String("service-name", "", "ServiceManager name to use instead of descriptor discovery")

	return cmd
}
