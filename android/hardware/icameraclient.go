package hardware

import (
	"context"
	"fmt"
	"github.com/xaionaro-go/binder/binder"
	"github.com/xaionaro-go/binder/parcel"
)

// Code generated by aidlgen. DO NOT EDIT.

const DescriptorICameraClient = "android.hardware.ICameraClient"

type ICameraClient interface {
	AsBinder() binder.IBinder
}

type CameraClientProxy struct {
	remote binder.IBinder
}

func NewCameraClientProxy(
	remote binder.IBinder,
) *CameraClientProxy {
	return &CameraClientProxy{remote: remote}
}

func (p *CameraClientProxy) AsBinder() binder.IBinder {
	return p.remote
}

var _ ICameraClient = (*CameraClientProxy)(nil)

// CameraClientStub dispatches incoming binder transactions
// to a typed ICameraClient implementation.
type CameraClientStub struct {
	Impl ICameraClient
}

var _ binder.TransactionReceiver = (*CameraClientStub)(nil)

func (s *CameraClientStub) OnTransaction(
	ctx context.Context,
	code binder.TransactionCode,
	_data *parcel.Parcel,
) (*parcel.Parcel, error) {
	switch code {
	default:
		return nil, fmt.Errorf("unknown transaction code %d", code)
	}
}

// ICameraClientServer is the server-side interface that user implementations
// provide to NewCameraClientStub. It contains only the business methods,
// without AsBinder (which is provided by the stub itself).
type ICameraClientServer interface {
}

type cameraClientStubWrapper struct {
	impl       ICameraClientServer
	stubBinder *binder.StubBinder
}

func (w *cameraClientStubWrapper) AsBinder() binder.IBinder {
	return w.stubBinder
}

var _ ICameraClient = (*cameraClientStubWrapper)(nil)

// NewCameraClientStub creates a server-side ICameraClient wrapping the given
// server implementation. The returned value satisfies ICameraClient
// and can be passed to proxy methods; its AsBinder() returns a
// *binder.StubBinder that is auto-registered with the binder
// driver on first use.
func NewCameraClientStub(
	impl ICameraClientServer,
) ICameraClient {
	wrapper := &cameraClientStubWrapper{impl: impl}
	stub := &CameraClientStub{Impl: wrapper}
	wrapper.stubBinder = binder.NewStubBinder(stub)
	return wrapper
}
