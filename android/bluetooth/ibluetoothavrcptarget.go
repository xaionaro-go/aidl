package bluetooth

import (
	"context"
	"fmt"
	"github.com/xaionaro-go/binder/binder"
	"github.com/xaionaro-go/binder/parcel"
)

// Code generated by aidlgen. DO NOT EDIT.

const DescriptorIBluetoothAvrcpTarget = "android.bluetooth.IBluetoothAvrcpTarget"

const (
	TransactionIBluetoothAvrcpTargetSendVolumeChanged = binder.FirstCallTransaction + 0
)

const (
	MethodIBluetoothAvrcpTargetSendVolumeChanged = "sendVolumeChanged"
)

type IBluetoothAvrcpTarget interface {
	AsBinder() binder.IBinder
	SendVolumeChanged(ctx context.Context, volume int32) error
}

type BluetoothAvrcpTargetProxy struct {
	Remote binder.IBinder
}

func NewBluetoothAvrcpTargetProxy(
	remote binder.IBinder,
) *BluetoothAvrcpTargetProxy {
	return &BluetoothAvrcpTargetProxy{Remote: remote}
}

func (p *BluetoothAvrcpTargetProxy) AsBinder() binder.IBinder {
	return p.Remote
}

var _ IBluetoothAvrcpTarget = (*BluetoothAvrcpTargetProxy)(nil)

func (p *BluetoothAvrcpTargetProxy) SendVolumeChanged(
	ctx context.Context,
	volume int32,
) error {
	_data := parcel.New()
	defer _data.Recycle()
	_data.WriteInterfaceToken(DescriptorIBluetoothAvrcpTarget)
	_data.WriteInt32(volume)

	_code, _err := p.Remote.ResolveCode(ctx, DescriptorIBluetoothAvrcpTarget, MethodIBluetoothAvrcpTargetSendVolumeChanged)
	if _err != nil {
		return fmt.Errorf("resolving %s.%s: %w", DescriptorIBluetoothAvrcpTarget, MethodIBluetoothAvrcpTargetSendVolumeChanged, _err)
	}

	_reply, _err := p.Remote.Transact(ctx, _code, 0, _data)
	if _err != nil {
		return _err
	}
	defer _reply.Recycle()

	if _err = binder.ReadStatus(_reply); _err != nil {
		return _err
	}

	return nil
}

// BluetoothAvrcpTargetStub dispatches incoming binder transactions
// to a typed IBluetoothAvrcpTarget implementation.
type BluetoothAvrcpTargetStub struct {
	Impl      IBluetoothAvrcpTarget
	Transport binder.VersionAwareTransport
}

var _ binder.TransactionReceiver = (*BluetoothAvrcpTargetStub)(nil)

func (s *BluetoothAvrcpTargetStub) Descriptor() string {
	return DescriptorIBluetoothAvrcpTarget
}

func (s *BluetoothAvrcpTargetStub) OnTransaction(
	ctx context.Context,
	code binder.TransactionCode,
	_data *parcel.Parcel,
) (*parcel.Parcel, error) {
	if _, _err := _data.ReadInterfaceToken(); _err != nil {
		return nil, _err
	}

	switch code {
	case TransactionIBluetoothAvrcpTargetSendVolumeChanged:
		_arg_volume, _err := _data.ReadInt32()
		if _err != nil {
			return nil, _err
		}
		_err = s.Impl.SendVolumeChanged(ctx, _arg_volume)
		_reply := parcel.New()
		if _err != nil {
			binder.WriteStatus(_reply, _err)
			return _reply, nil
		}
		binder.WriteStatus(_reply, nil)
		return _reply, nil
	default:
		return nil, fmt.Errorf("unknown transaction code %d", code)
	}
}

// IBluetoothAvrcpTargetServer is the server-side interface that user implementations
// provide to NewBluetoothAvrcpTargetStub. It contains only the business methods,
// without AsBinder (which is provided by the stub itself).
type IBluetoothAvrcpTargetServer interface {
	SendVolumeChanged(ctx context.Context, volume int32) error
}

type bluetoothAvrcpTargetStubWrapper struct {
	impl       IBluetoothAvrcpTargetServer
	stubBinder *binder.StubBinder
}

func (w *bluetoothAvrcpTargetStubWrapper) AsBinder() binder.IBinder {
	return w.stubBinder
}

func (w *bluetoothAvrcpTargetStubWrapper) SendVolumeChanged(
	ctx context.Context,
	volume int32,
) error {
	return w.impl.SendVolumeChanged(ctx, volume)
}

var _ IBluetoothAvrcpTarget = (*bluetoothAvrcpTargetStubWrapper)(nil)

// NewBluetoothAvrcpTargetStub creates a server-side IBluetoothAvrcpTarget wrapping the given
// server implementation. The returned value satisfies IBluetoothAvrcpTarget
// and can be passed to proxy methods; its AsBinder() returns a
// *binder.StubBinder that is auto-registered with the binder
// driver on first use.
func NewBluetoothAvrcpTargetStub(
	impl IBluetoothAvrcpTargetServer,
) IBluetoothAvrcpTarget {
	wrapper := &bluetoothAvrcpTargetStubWrapper{impl: impl}
	stub := &BluetoothAvrcpTargetStub{Impl: wrapper}
	wrapper.stubBinder = binder.NewStubBinder(stub)
	return wrapper
}
