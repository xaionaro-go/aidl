package scan

import (
	"context"
	"fmt"
	os "github.com/xaionaro-go/binder/android/os"
	"github.com/xaionaro-go/binder/binder"
	"github.com/xaionaro-go/binder/parcel"
)

// Code generated by aidlgen. DO NOT EDIT.

const DescriptorILcnConflictListener = "android.media.tv.extension.scan.ILcnConflictListener"

const (
	TransactionILcnConflictListenerOnDetectLcnConflict = binder.FirstCallTransaction + 0
)

type ILcnConflictListener interface {
	AsBinder() binder.IBinder
	OnDetectLcnConflict(ctx context.Context, detectLcnConflicts os.Bundle) error
}

type LcnConflictListenerProxy struct {
	remote binder.IBinder
}

func NewLcnConflictListenerProxy(
	remote binder.IBinder,
) *LcnConflictListenerProxy {
	return &LcnConflictListenerProxy{remote: remote}
}

func (p *LcnConflictListenerProxy) AsBinder() binder.IBinder {
	return p.remote
}

var _ ILcnConflictListener = (*LcnConflictListenerProxy)(nil)

func (p *LcnConflictListenerProxy) OnDetectLcnConflict(
	ctx context.Context,
	detectLcnConflicts os.Bundle,
) error {
	_data := parcel.New()
	_data.WriteInterfaceToken(DescriptorILcnConflictListener)
	_data.WriteInt32(1)
	if _err := detectLcnConflicts.MarshalParcel(_data); _err != nil {
		return _err
	}

	_code, _err := p.remote.ResolveCode(DescriptorILcnConflictListener, "onDetectLcnConflict")
	if _err != nil {
		_code = TransactionILcnConflictListenerOnDetectLcnConflict
	}

	_, _err = p.remote.Transact(ctx, _code, binder.FlagOneway, _data)
	return _err
}

// LcnConflictListenerStub dispatches incoming binder transactions
// to a typed ILcnConflictListener implementation.
type LcnConflictListenerStub struct {
	Impl ILcnConflictListener
}

var _ binder.TransactionReceiver = (*LcnConflictListenerStub)(nil)

func (s *LcnConflictListenerStub) OnTransaction(
	ctx context.Context,
	code binder.TransactionCode,
	_data *parcel.Parcel,
) (*parcel.Parcel, error) {
	switch code {
	case TransactionILcnConflictListenerOnDetectLcnConflict:
		if _, _err := _data.ReadString16(); _err != nil {
			return nil, _err
		}
		var _arg_detectLcnConflicts os.Bundle
		{
			_nullInd, _err := _data.ReadInt32()
			if _err != nil {
				return nil, _err
			}
			if _nullInd != 0 {
				if _err = _arg_detectLcnConflicts.UnmarshalParcel(_data); _err != nil {
					return nil, _err
				}
			}
		}
		_err := s.Impl.OnDetectLcnConflict(ctx, _arg_detectLcnConflicts)
		_ = _err
		return nil, nil
	default:
		return nil, fmt.Errorf("unknown transaction code %d", code)
	}
}

// ILcnConflictListenerServer is the server-side interface that user implementations
// provide to NewLcnConflictListenerStub. It contains only the business methods,
// without AsBinder (which is provided by the stub itself).
type ILcnConflictListenerServer interface {
	OnDetectLcnConflict(ctx context.Context, detectLcnConflicts os.Bundle) error
}

type lcnConflictListenerStubWrapper struct {
	impl       ILcnConflictListenerServer
	stubBinder *binder.StubBinder
}

func (w *lcnConflictListenerStubWrapper) AsBinder() binder.IBinder {
	return w.stubBinder
}

func (w *lcnConflictListenerStubWrapper) OnDetectLcnConflict(
	ctx context.Context,
	detectLcnConflicts os.Bundle,
) error {
	return w.impl.OnDetectLcnConflict(ctx, detectLcnConflicts)
}

var _ ILcnConflictListener = (*lcnConflictListenerStubWrapper)(nil)

// NewLcnConflictListenerStub creates a server-side ILcnConflictListener wrapping the given
// server implementation. The returned value satisfies ILcnConflictListener
// and can be passed to proxy methods; its AsBinder() returns a
// *binder.StubBinder that is auto-registered with the binder
// driver on first use.
func NewLcnConflictListenerStub(
	impl ILcnConflictListenerServer,
) ILcnConflictListener {
	wrapper := &lcnConflictListenerStubWrapper{impl: impl}
	stub := &LcnConflictListenerStub{Impl: wrapper}
	wrapper.stubBinder = binder.NewStubBinder(stub)
	return wrapper
}
