package soundtrigger_middleware

import (
	"context"
	"fmt"
	"github.com/xaionaro-go/binder/binder"
	"github.com/xaionaro-go/binder/parcel"
)

// Code generated by aidlgen. DO NOT EDIT.

const DescriptorIAcknowledgeEvent = "android.media.soundtrigger_middleware.IAcknowledgeEvent"

const (
	TransactionIAcknowledgeEventEventReceived = binder.FirstCallTransaction + 0
)

type IAcknowledgeEvent interface {
	AsBinder() binder.IBinder
	EventReceived(ctx context.Context) error
}

type AcknowledgeEventProxy struct {
	remote binder.IBinder
}

func NewAcknowledgeEventProxy(
	remote binder.IBinder,
) *AcknowledgeEventProxy {
	return &AcknowledgeEventProxy{remote: remote}
}

func (p *AcknowledgeEventProxy) AsBinder() binder.IBinder {
	return p.remote
}

var _ IAcknowledgeEvent = (*AcknowledgeEventProxy)(nil)

func (p *AcknowledgeEventProxy) EventReceived(
	ctx context.Context,
) error {
	_data := parcel.New()
	_data.WriteInterfaceToken(DescriptorIAcknowledgeEvent)

	_code, _err := p.remote.ResolveCode(DescriptorIAcknowledgeEvent, "eventReceived")
	if _err != nil {
		_code = TransactionIAcknowledgeEventEventReceived
	}

	_, _err = p.remote.Transact(ctx, _code, binder.FlagOneway, _data)
	return _err
}

// AcknowledgeEventStub dispatches incoming binder transactions
// to a typed IAcknowledgeEvent implementation.
type AcknowledgeEventStub struct {
	Impl IAcknowledgeEvent
}

var _ binder.TransactionReceiver = (*AcknowledgeEventStub)(nil)

func (s *AcknowledgeEventStub) OnTransaction(
	ctx context.Context,
	code binder.TransactionCode,
	_data *parcel.Parcel,
) (*parcel.Parcel, error) {
	switch code {
	case TransactionIAcknowledgeEventEventReceived:
		if _, _err := _data.ReadString16(); _err != nil {
			return nil, _err
		}
		_err := s.Impl.EventReceived(ctx)
		_ = _err
		return nil, nil
	default:
		return nil, fmt.Errorf("unknown transaction code %d", code)
	}
}

// IAcknowledgeEventServer is the server-side interface that user implementations
// provide to NewAcknowledgeEventStub. It contains only the business methods,
// without AsBinder (which is provided by the stub itself).
type IAcknowledgeEventServer interface {
	EventReceived(ctx context.Context) error
}

type acknowledgeEventStubWrapper struct {
	impl       IAcknowledgeEventServer
	stubBinder *binder.StubBinder
}

func (w *acknowledgeEventStubWrapper) AsBinder() binder.IBinder {
	return w.stubBinder
}

func (w *acknowledgeEventStubWrapper) EventReceived(
	ctx context.Context,
) error {
	return w.impl.EventReceived(ctx)
}

var _ IAcknowledgeEvent = (*acknowledgeEventStubWrapper)(nil)

// NewAcknowledgeEventStub creates a server-side IAcknowledgeEvent wrapping the given
// server implementation. The returned value satisfies IAcknowledgeEvent
// and can be passed to proxy methods; its AsBinder() returns a
// *binder.StubBinder that is auto-registered with the binder
// driver on first use.
func NewAcknowledgeEventStub(
	impl IAcknowledgeEventServer,
) IAcknowledgeEvent {
	wrapper := &acknowledgeEventStubWrapper{impl: impl}
	stub := &AcknowledgeEventStub{Impl: wrapper}
	wrapper.stubBinder = binder.NewStubBinder(stub)
	return wrapper
}
